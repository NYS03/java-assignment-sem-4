/*public class cartQuantityFix {
    public static int cartQuantityFix(int i , int newQuantity, int quantity){
        for (i = 0; i < cart.cart.length; i++) {
            if (cart.cart[i] != null) {
                newQuantity = quantity + globalInventory.cpu[i].getQuantity();
            }
        }
        return newQuantity;
    }
}
*/